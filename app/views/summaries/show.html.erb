<h1><%= @summary.title %></h1>

<div class="row">
  <div class="col-md-8">
    <p><%= @summary.body %></p>
  </div>
  
  <div class="col-md-4">
  <% if policy(@post).edit? %>
    <%= link_to "Edit", edit_topic_summary_path(@topic, @post), class: 'btn btn-success' %>
  <% end %>
  </div>
  
</div>
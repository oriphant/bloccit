<h1>Edit Summary</h1>
<div class="row">
  <div class="col-md-4">
    <p>Guidelines for Summaries</p>
    <ul>
      <li>There are none</li>
      <li>I don't even know why I have a summary</li>
    </ul>
  </div>
  <div class="col-md-8">
    <%=form_for [@topic, @post, @summary] do |f| %>
      <div class="form-group">
        <%= f.label :title %>
        <%= f.text_field :title, class: 'form-control', placeholder: "Enter summary title" %>
      </div>
      <div class="form-group">
        <%= f.label :body %>
        <%= f.text_area :body, rows: 8, class: 'form-control', placeholder: "Enter summary body" %>
      </div>
      <div class="form-group">
        <%= f.submit "Save", class: 'btn btn-success' %>
      </div>
    <%end%>
  </div>
</div>